<p id="notice"><%= notice %></p>

<h1>Listing Arts</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Art type</th>
      <th>User</th>
      <th>Price</th>
      <th>Status</th>
      <th>Quantity</th>
      <th>Images</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @arts.each do |art| %>
      <tr>
        <td><%= art.name %></td>
        <td><%= art.art_type_id %></td>
        <td><%= art.user_id %></td>
        <td><%= art.price %></td>
        <td><%= art.status %></td>
        <td><%= art.quantity %></td>
        <td><%= art.images %></td>
        <td><%= link_to 'Show', art %></td>
        <td><%= link_to 'Edit', edit_art_path(art) %></td>
        <td><%= link_to 'Destroy', art, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Art', new_art_path %>
